apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: openhammer
  labels:
    app: openhammer
    tier: server
spec:
  selector:
    app: openhammer
    tier: server
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
